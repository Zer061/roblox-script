--[[
	WARNING: Heads up! This script has not been verified by ScriptBlox. Use at your own risk!
]]
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "NoobKiller"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- Bố cục gọn: header 30px + phần thân, mỗi hàng cao 26 cách nhau 4
local HEADER = 30
local CONTENT_HEIGHT = 304

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 210, 0, HEADER + CONTENT_HEIGHT)
frame.Position = UDim2.new(1, -220, 0.5, -(HEADER + CONTENT_HEIGHT) / 2)
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 10)
corner.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -34, 0, HEADER)
title.Position = UDim2.new(0, 8, 0, 0)
title.BackgroundTransparency = 1
title.Text = "NOOB KILLER"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextXAlignment = Enum.TextXAlignment.Left
title.TextSize = 14
title.Font = Enum.Font.GothamBold
title.Parent = frame

local content = Instance.new("Frame")
content.Size = UDim2.new(1, 0, 0, CONTENT_HEIGHT)
content.Position = UDim2.new(0, 0, 0, HEADER)
content.BackgroundTransparency = 1
content.Parent = frame

local minimize = Instance.new("TextButton")
minimize.Size = UDim2.new(0, 22, 0, 20)
minimize.Position = UDim2.new(1, -28, 0, 5)
minimize.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
minimize.BorderSizePixel = 0
minimize.Text = "-"
minimize.TextColor3 = Color3.fromRGB(255, 255, 255)
minimize.TextSize = 16
minimize.Font = Enum.Font.GothamBold
minimize.Parent = frame

local minimizeCorner = Instance.new("UICorner")
minimizeCorner.CornerRadius = UDim.new(0, 6)
minimizeCorner.Parent = minimize

minimize.MouseButton1Click:Connect(function()
	content.Visible = not content.Visible
	minimize.Text = content.Visible and "-" or "+"
	frame.Size = UDim2.new(0, 210, 0, content.Visible and HEADER + CONTENT_HEIGHT or HEADER)
end)

local function makeButton(text, position, size, color)
	local button = Instance.new("TextButton")
	button.Size = size
	button.Position = position
	button.BackgroundColor3 = color
	button.BorderSizePixel = 0
	button.Text = text
	button.TextColor3 = Color3.fromRGB(255, 255, 255)
	button.TextSize = 12
	button.Font = Enum.Font.GothamBold
	button.AutoButtonColor = true
	button.Parent = content

	local buttonCorner = Instance.new("UICorner")
	buttonCorner.CornerRadius = UDim.new(0, 6)
	buttonCorner.Parent = button

	return button
end

-- Vị trí chuẩn cho từng kiểu ô
local SIZE_FULL = UDim2.new(1, -16, 0, 26)
local SIZE_HALF = UDim2.new(0.5, -10, 0, 26)

local function atLeft(y)
	return UDim2.new(0, 8, 0, y)
end

local function atRight(y)
	return UDim2.new(0.5, 2, 0, y)
end

local function makeValueRow(y, text)
	local minus = makeButton("-", UDim2.new(0, 8, 0, y), UDim2.new(0, 28, 0, 26), Color3.fromRGB(50, 50, 50))
	local plus = makeButton("+", UDim2.new(1, -36, 0, y), UDim2.new(0, 28, 0, 26), Color3.fromRGB(50, 50, 50))

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, -88, 0, 26)
	label.Position = UDim2.new(0, 44, 0, y)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Color3.fromRGB(230, 230, 230)
	label.TextSize = 13
	label.Font = Enum.Font.GothamBold
	label.Parent = content

	return minus, plus, label
end

local toggle = makeButton("NOOB KILL: OFF", atLeft(0), SIZE_FULL, Color3.fromRGB(170, 45, 45))

local flyToggle = makeButton("FLY: OFF", atLeft(30), SIZE_HALF, Color3.fromRGB(170, 45, 45))
local brightToggle = makeButton("LIGHT: OFF", atRight(30), SIZE_HALF, Color3.fromRGB(170, 45, 45))

local attackToggle = makeButton("ATK: OFF", atLeft(60), SIZE_HALF, Color3.fromRGB(170, 45, 45))
local farmToggle = makeButton("FARM: OFF", atRight(60), SIZE_HALF, Color3.fromRGB(170, 45, 45))

local modeToggle = makeButton("DMG: HP=0 (khong coin)", atLeft(90), SIZE_FULL, Color3.fromRGB(120, 90, 40))

local speedDown, speedUp, speedLabel = makeValueRow(120, "Speed: 60")
local rangeDown, rangeUp, rangeLabel = makeValueRow(150, "Range: 60")
local keepDown, keepUp, keepLabel = makeValueRow(180, "Keep: 18")

local upButton = makeButton("UP", atLeft(210), SIZE_HALF, Color3.fromRGB(50, 50, 50))
local downButton = makeButton("DOWN", atRight(210), SIZE_HALF, Color3.fromRGB(50, 50, 50))

local hpLabel = Instance.new("TextLabel")
hpLabel.Size = UDim2.new(1, -16, 0, 16)
hpLabel.Position = UDim2.new(0, 8, 0, 240)
hpLabel.BackgroundTransparency = 1
hpLabel.Text = "Retreat HP: 30%"
hpLabel.TextColor3 = Color3.fromRGB(230, 230, 230)
hpLabel.TextXAlignment = Enum.TextXAlignment.Left
hpLabel.TextSize = 12
hpLabel.Font = Enum.Font.GothamBold
hpLabel.Parent = content

-- vùng bấm cao 18px cho dễ kéo, thanh nhìn thấy chỉ 8px
local sliderTrack = Instance.new("Frame")
sliderTrack.Size = UDim2.new(1, -16, 0, 18)
sliderTrack.Position = UDim2.new(0, 8, 0, 258)
sliderTrack.BackgroundTransparency = 1
sliderTrack.Active = true
sliderTrack.Parent = content

local sliderBar = Instance.new("Frame")
sliderBar.Size = UDim2.new(1, 0, 0, 8)
sliderBar.Position = UDim2.new(0, 0, 0.5, -4)
sliderBar.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
sliderBar.BorderSizePixel = 0
sliderBar.Parent = sliderTrack

local sliderBarCorner = Instance.new("UICorner")
sliderBarCorner.CornerRadius = UDim.new(1, 0)
sliderBarCorner.Parent = sliderBar

local sliderFill = Instance.new("Frame")
sliderFill.Size = UDim2.new(0.3, 0, 1, 0)
sliderFill.BackgroundColor3 = Color3.fromRGB(210, 70, 70)
sliderFill.BorderSizePixel = 0
sliderFill.Parent = sliderBar

local sliderFillCorner = Instance.new("UICorner")
sliderFillCorner.CornerRadius = UDim.new(1, 0)
sliderFillCorner.Parent = sliderFill

local sliderKnob = Instance.new("Frame")
sliderKnob.Size = UDim2.new(0, 14, 0, 14)
sliderKnob.AnchorPoint = Vector2.new(0.5, 0.5)
sliderKnob.Position = UDim2.new(0.3, 0, 0.5, 0)
sliderKnob.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
sliderKnob.BorderSizePixel = 0
sliderKnob.Parent = sliderBar

local sliderKnobCorner = Instance.new("UICorner")
sliderKnobCorner.CornerRadius = UDim.new(1, 0)
sliderKnobCorner.Parent = sliderKnob

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -16, 0, 16)
statusLabel.Position = UDim2.new(0, 8, 0, 280)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "Target: none"
statusLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.TextSize = 11
statusLabel.Font = Enum.Font.Gotham
statusLabel.TextTruncate = Enum.TextTruncate.AtEnd
statusLabel.Parent = content

-- ===== RETREAT (rút lui hồi máu) =====

local retreatPercent = 30 -- máu dưới mức này thì bay lên trời
local retreatAltitude = 1000 -- bay cao thêm bấy nhiêu studs
local retreating = false
local retreatAnchor = nil -- điểm treo trên trời khi đang chờ hồi máu

local function setRetreatPercent(value)
	retreatPercent = math.clamp(math.floor(value + 0.5), 0, 100)

	local alpha = retreatPercent / 100
	hpLabel.Text = "Retreat HP: " .. retreatPercent .. "%"
	sliderFill.Size = UDim2.new(alpha, 0, 1, 0)
	sliderKnob.Position = UDim2.new(alpha, 0, 0.5, 0)
end

do
	local dragging = false

	local function updateFromInput(input)
		local origin = sliderBar.AbsolutePosition.X
		local width = math.max(sliderBar.AbsoluteSize.X, 1)

		setRetreatPercent(math.clamp((input.Position.X - origin) / width, 0, 1) * 100)
	end

	sliderTrack.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			updateFromInput(input)
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement
			or input.UserInputType == Enum.UserInputType.Touch) then
			updateFromInput(input)
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
		end
	end)
end

local enabled = false

local function isNoobFolder(instance)
	if not instance:IsA("Folder") then
		return false
	end

	local name = instance.Name:lower()

	return name == "allnoobs" or name:find("noob", 1, true) ~= nil
end

local function killNoobs()
	for _, object in ipairs(Workspace:GetDescendants()) do
		if isNoobFolder(object) then
			for _, descendant in ipairs(object:GetDescendants()) do
				if descendant:IsA("Humanoid") then
					descendant.Health = 0
				end
			end
		end
	end
end

toggle.MouseButton1Click:Connect(function()
	enabled = not enabled

	if enabled then
		toggle.Text = "NOOB KILL: ON"
		toggle.BackgroundColor3 = Color3.fromRGB(45, 170, 75)
		killNoobs()
	else
		toggle.Text = "NOOB KILL: OFF"
		toggle.BackgroundColor3 = Color3.fromRGB(170, 45, 45)
	end
end)

task.spawn(function()
	while gui.Parent do
		if enabled then
			killNoobs()
		end

		task.wait(0.15)
	end
end)

-- ===== FLY =====

local flying = false
local flySpeed = 60
local holdingUp = false
local holdingDown = false
local aimTarget = nil -- được Auto Attack / Auto Farm set, dùng chung với BodyGyro khi đang bay
local autoFarm = false
local farmDestination = nil -- Vector3 điểm bay tới khi đang farm
local farmAwayDir = nil -- hướng đứng so với mục tiêu, tính lại mỗi tick farm
local farmHoverDistance = 2 -- sai số cho phép quanh điểm đứng
local keepDistance = 18 -- khoảng cách ngang luôn giữ với mục tiêu
local hoverHeight = 14 -- bay cao hơn mục tiêu bấy nhiêu studs

-- Chế độ gây damage: HP=0 (nhanh, KHONG duoc coin) hoặc vũ khí (chậm hơn, có coin)
local useWeapon = false
local standoffDistance = keepDistance -- khoảng cách đang thực sự dùng
local standoffHeight = hoverHeight

-- Khoảng cách đứng do slider Keep quyết định ở cả 2 chế độ:
-- súng thì để xa cho an toàn, cận chiến thì kéo Keep xuống ~5 cho chạm được.
local function refreshStandoff()
	standoffDistance = keepDistance
	standoffHeight = hoverHeight
end

local bodyVelocity, bodyGyro, flyConnection

local function getCharacterParts()
	local character = player.Character

	if not character then
		return nil
	end

	local root = character:FindFirstChild("HumanoidRootPart")
	local humanoid = character:FindFirstChildOfClass("Humanoid")

	if not root or not humanoid or humanoid.Health <= 0 then
		return nil
	end

	return root, humanoid
end

local function getFlyDirection(root, humanoid)
	local camera = Workspace.CurrentCamera
	local direction = Vector3.zero

	-- Rút lui hồi máu: ưu tiên cao nhất, kể cả khi đang farm
	if retreating and retreatAnchor then
		local offset = retreatAnchor - root.Position

		if offset.Magnitude <= 4 then
			return Vector3.zero
		end

		return offset.Unit * math.clamp(offset.Magnitude / 12, 0.15, 1)
	end

	-- Auto Farm lái thay người chơi
	if autoFarm and farmDestination then
		local destination = farmDestination

		-- bám mục tiêu theo từng frame, không đợi tick farm 0.4s
		if farmAwayDir and aimTarget and aimTarget.Parent then
			destination = aimTarget.Position + farmAwayDir * standoffDistance + Vector3.new(0, standoffHeight, 0)
		end

		local offset = destination - root.Position
		local distance = offset.Magnitude

		if distance <= farmHoverDistance then
			return Vector3.zero
		end

		-- giảm tốc khi tới gần điểm đứng để không vọt qua rồi dao động
		return offset.Unit * math.clamp(distance / 12, 0.08, 1)
	end

	if camera then
		local cf = camera.CFrame

		if UserInputService.KeyboardEnabled and not UserInputService:GetFocusedTextBox() then
			if UserInputService:IsKeyDown(Enum.KeyCode.W) then
				direction += cf.LookVector
			end
			if UserInputService:IsKeyDown(Enum.KeyCode.S) then
				direction -= cf.LookVector
			end
			if UserInputService:IsKeyDown(Enum.KeyCode.A) then
				direction -= cf.RightVector
			end
			if UserInputService:IsKeyDown(Enum.KeyCode.D) then
				direction += cf.RightVector
			end
		end
	end

	-- Mobile joystick / gamepad fallback
	if direction.Magnitude == 0 then
		direction = humanoid.MoveDirection
	end

	if holdingUp or UserInputService:IsKeyDown(Enum.KeyCode.Space) then
		direction += Vector3.new(0, 1, 0)
	end
	if holdingDown or UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
		direction -= Vector3.new(0, 1, 0)
	end

	if direction.Magnitude > 0 then
		return direction.Unit
	end

	return Vector3.zero
end

local function stopFly()
	flying = false

	if flyConnection then
		flyConnection:Disconnect()
		flyConnection = nil
	end

	if bodyVelocity then
		bodyVelocity:Destroy()
		bodyVelocity = nil
	end

	if bodyGyro then
		bodyGyro:Destroy()
		bodyGyro = nil
	end

	local _, humanoid = getCharacterParts()

	if humanoid then
		humanoid.PlatformStand = false
	end

	flyToggle.Text = "FLY: OFF"
	flyToggle.BackgroundColor3 = Color3.fromRGB(170, 45, 45)
end

local function startFly()
	local root, humanoid = getCharacterParts()

	if not root then
		return
	end

	flying = true

	bodyVelocity = Instance.new("BodyVelocity")
	bodyVelocity.Name = "FlyVelocity"
	bodyVelocity.MaxForce = Vector3.new(1, 1, 1) * 9e9
	bodyVelocity.P = 9e4
	bodyVelocity.Velocity = Vector3.zero
	bodyVelocity.Parent = root

	bodyGyro = Instance.new("BodyGyro")
	bodyGyro.Name = "FlyGyro"
	bodyGyro.MaxTorque = Vector3.new(1, 1, 1) * 9e9
	bodyGyro.P = 9e4
	bodyGyro.D = 500
	bodyGyro.CFrame = root.CFrame
	bodyGyro.Parent = root

	flyToggle.Text = "FLY: ON"
	flyToggle.BackgroundColor3 = Color3.fromRGB(45, 170, 75)

	flyConnection = RunService.RenderStepped:Connect(function()
		local currentRoot, currentHumanoid = getCharacterParts()

		if not currentRoot or currentRoot ~= root then
			stopFly()
			return
		end

		-- Không rơi khi đứng yên
		currentHumanoid:ChangeState(Enum.HumanoidStateType.Freefall)

		bodyVelocity.Velocity = getFlyDirection(currentRoot, currentHumanoid) * flySpeed

		local look

		if aimTarget and aimTarget.Parent then
			look = (aimTarget.Position - currentRoot.Position) * Vector3.new(1, 0, 1)
		else
			local camera = Workspace.CurrentCamera
			look = camera and camera.CFrame.LookVector * Vector3.new(1, 0, 1) or nil
		end

		if look and look.Magnitude > 0.01 then
			bodyGyro.CFrame = CFrame.lookAt(currentRoot.Position, currentRoot.Position + look.Unit)
		end
	end)
end

local function toggleFly()
	if flying then
		stopFly()
	else
		startFly()
	end
end

flyToggle.MouseButton1Click:Connect(toggleFly)

local function bindHold(button, setter)
	button.MouseButton1Down:Connect(function()
		setter(true)
	end)
	button.MouseButton1Up:Connect(function()
		setter(false)
	end)
	button.MouseLeave:Connect(function()
		setter(false)
	end)
end

bindHold(upButton, function(state)
	holdingUp = state
end)

bindHold(downButton, function(state)
	holdingDown = state
end)

local function setSpeed(value)
	flySpeed = math.clamp(value, 10, 500)
	speedLabel.Text = "Speed: " .. flySpeed
end

speedUp.MouseButton1Click:Connect(function()
	setSpeed(flySpeed + 10)
end)

speedDown.MouseButton1Click:Connect(function()
	setSpeed(flySpeed - 10)
end)

-- ===== AUTO ATTACK =====

local autoAttacking = false
local attackRange = 60
local attackCooldown = 0.2

local blacklist = {} -- [model] = thời điểm hết hạn, cho mục tiêu không tiếp cận được

local function getTargetRoot(humanoid)
	if humanoid.Health <= 0 then
		return nil
	end

	local model = humanoid.Parent

	if not model or not model:IsA("Model") or model == player.Character then
		return nil
	end

	-- bỏ qua người chơi thật, chỉ đánh NPC/noob
	if Players:GetPlayerFromCharacter(model) then
		return nil
	end

	local expiry = blacklist[model]

	if expiry then
		if os.clock() < expiry then
			return nil
		end

		blacklist[model] = nil
	end

	return model:FindFirstChild("HumanoidRootPart") or model.PrimaryPart
end

-- Quét 1 lần, trả về danh sách mục tiêu hợp lệ trong tầm
local function collectTargets(root, maxDistance)
	local targets = {}

	for _, object in ipairs(Workspace:GetDescendants()) do
		if object:IsA("Humanoid") then
			local targetRoot = getTargetRoot(object)

			if targetRoot then
				local distance = (targetRoot.Position - root.Position).Magnitude

				if distance <= maxDistance then
					table.insert(targets, {
						root = targetRoot,
						model = object.Parent,
						name = object.Parent.Name,
						distance = distance,
					})
				end
			end
		end
	end

	return targets
end

local function getNearestTarget(root)
	local best

	for _, target in ipairs(collectTargets(root, attackRange)) do
		if not best or target.distance < best.distance then
			best = target
		end
	end

	if not best then
		return nil
	end

	return best.root, best.name, best.distance
end

local function getEquippedTool(character, humanoid)
	local tool = character:FindFirstChildOfClass("Tool")

	if tool then
		return tool
	end

	local backpack = player:FindFirstChildOfClass("Backpack")
	local stored = backpack and backpack:FindFirstChildOfClass("Tool")

	if not stored then
		return nil
	end

	humanoid:EquipTool(stored)

	if stored.Parent ~= character then
		stored.Parent = character
	end

	return character:FindFirstChildOfClass("Tool")
end

-- Súng loại giữ chuột: phải GIỮ nút xuống, nhấn-nhả trong 1 frame game không đọc kịp.
local GuiService = game:GetService("GuiService")
local virtualUser, virtualInput

pcall(function()
	virtualUser = game:GetService("VirtualUser")
end)

pcall(function()
	virtualInput = game:GetService("VirtualInputManager")
end)

local fireMethod = "?"
local aimCameraAtTarget = true -- đặt false nếu không muốn camera bị giật về mục tiêu
local mouseHeld = false
local heldMethod = nil
local lastFireRequest = 0

local function executorGlobal(name)
	local value

	pcall(function()
		local env = (getgenv and getgenv()) or getfenv()
		value = rawget(env, name)
	end)

	if type(value) == "function" then
		return value
	end

	return nil
end

local function moveVirtualMouse(screenPoint)
	local moveMouse = executorGlobal("mousemoveabs")

	if moveMouse then
		pcall(moveMouse, screenPoint.X, screenPoint.Y)
		return
	end

	if virtualInput then
		pcall(function()
			virtualInput:SendMouseMoveEvent(screenPoint.X, screenPoint.Y, game)
		end)
	end
end

local function holdMouse(screenPoint, cameraCFrame)
	moveVirtualMouse(screenPoint)

	if mouseHeld then
		return -- đang giữ rồi, không nhấn lại kẻo thành nhả
	end

	local press = executorGlobal("mouse1press")

	if press and pcall(press) then
		mouseHeld, heldMethod, fireMethod = true, "executor", "mouse1press(giu)"
		return
	end

	if virtualInput then
		local ok = pcall(function()
			virtualInput:SendMouseButtonEvent(screenPoint.X, screenPoint.Y, 0, true, game, 1)
		end)

		if ok then
			mouseHeld, heldMethod, fireMethod = true, "vim", "VIM(giu)"
			return
		end
	end

	if virtualUser then
		local ok = pcall(function()
			virtualUser:Button1Down(screenPoint, cameraCFrame)
		end)

		if ok then
			mouseHeld, heldMethod, fireMethod = true, "virtualuser", "VirtualUser(giu)"
		end
	end
end

local function releaseMouse()
	if not mouseHeld then
		return
	end

	local camera = Workspace.CurrentCamera
	local screenPoint = camera and camera.ViewportSize / 2 or Vector2.new(0, 0)

	if heldMethod == "executor" then
		local release = executorGlobal("mouse1release")

		if release then
			pcall(release)
		end
	elseif heldMethod == "vim" and virtualInput then
		pcall(function()
			virtualInput:SendMouseButtonEvent(screenPoint.X, screenPoint.Y, 0, false, game, 1)
		end)
	elseif heldMethod == "virtualuser" and virtualUser then
		pcall(function()
			virtualUser:Button1Up(screenPoint, camera and camera.CFrame or CFrame.new())
		end)
	end

	mouseHeld = false
	heldMethod = nil
end

local function fireWeapon(tool, targetRoot)
	lastFireRequest = os.clock()

	-- Tool loại Activated vẫn dùng được, gọi song song cũng không hại
	pcall(function()
		tool:Activate()
	end)

	if not targetRoot then
		releaseMouse()
		return
	end

	local camera = Workspace.CurrentCamera

	if not camera then
		return
	end

	-- kéo camera về mục tiêu, không thì Mouse.Hit của game trỏ đi chỗ khác
	if aimCameraAtTarget then
		pcall(function()
			camera.CFrame = CFrame.lookAt(camera.CFrame.Position, targetRoot.Position)
		end)
	end

	local point, onScreen = camera:WorldToViewportPoint(targetRoot.Position)

	if not onScreen then
		releaseMouse() -- mục tiêu ra khỏi màn hình thì thả súng
		return
	end

	local inset = GuiService:GetGuiInset()

	holdMouse(Vector2.new(point.X, point.Y + inset.Y), camera.CFrame)
end

-- Không ai gọi fireWeapon nữa (hết mục tiêu / tắt farm / chết) thì tự nhả chuột
task.spawn(function()
	while gui.Parent do
		if mouseHeld and os.clock() - lastFireRequest > 0.5 then
			releaseMouse()
		end

		task.wait(0.2)
	end

	releaseMouse()
end)

local function attackStep()
	if autoFarm or retreating then
		return -- Auto Farm tự lo phần đánh; đang rút lui thì không đánh nữa
	end

	local root, humanoid = getCharacterParts()

	if not root then
		aimTarget = nil
		statusLabel.Text = "Target: none"
		return
	end

	local targetRoot, targetName, distance = getNearestTarget(root)

	aimTarget = targetRoot

	if not targetRoot then
		statusLabel.Text = "Target: none"
		return
	end

	statusLabel.Text = string.format("Target: %s (%.0f)", targetName, distance)

	-- quay mặt về mục tiêu (khi bay thì BodyGyro lo phần này)
	if not flying then
		local look = (targetRoot.Position - root.Position) * Vector3.new(1, 0, 1)

		if look.Magnitude > 0.01 then
			root.CFrame = CFrame.lookAt(root.Position, root.Position + look.Unit)
		end
	end

	local tool = getEquippedTool(player.Character, humanoid)

	if tool then
		fireWeapon(tool, targetRoot)
	end
end

local function setAutoAttack(state)
	autoAttacking = state

	if autoAttacking then
		attackToggle.Text = "ATK: ON"
		attackToggle.BackgroundColor3 = Color3.fromRGB(45, 170, 75)
	else
		attackToggle.Text = "ATK: OFF"
		attackToggle.BackgroundColor3 = Color3.fromRGB(170, 45, 45)
		releaseMouse()
		aimTarget = nil
		statusLabel.Text = "Target: none"
	end
end

attackToggle.MouseButton1Click:Connect(function()
	setAutoAttack(not autoAttacking)
end)

local function setRange(value)
	attackRange = math.clamp(value, 10, 500)
	rangeLabel.Text = "Range: " .. attackRange
end

rangeUp.MouseButton1Click:Connect(function()
	setRange(attackRange + 10)
end)

rangeDown.MouseButton1Click:Connect(function()
	setRange(attackRange - 10)
end)

local function setKeepDistance(value)
	keepDistance = math.clamp(value, 4, 120)
	hoverHeight = math.clamp(keepDistance * 0.8, 2, 60)
	keepLabel.Text = "Keep: " .. keepDistance
	refreshStandoff()
end

local function setUseWeapon(state)
	useWeapon = state

	if useWeapon then
		modeToggle.Text = "DMG: VU KHI (co coin)"
		modeToggle.BackgroundColor3 = Color3.fromRGB(45, 170, 75)

		-- tắt luôn kill HP=0, không thì nó cướp mạng mà chẳng ra coin
		if enabled then
			enabled = false
			toggle.Text = "NOOB KILL: OFF"
			toggle.BackgroundColor3 = Color3.fromRGB(170, 45, 45)
		end
	else
		modeToggle.Text = "DMG: HP=0 (khong coin)"
		modeToggle.BackgroundColor3 = Color3.fromRGB(120, 90, 40)
		releaseMouse()
	end

	refreshStandoff()
end

modeToggle.MouseButton1Click:Connect(function()
	setUseWeapon(not useWeapon)
end)

-- Chỉ khai hoả, để Auto Farm lo phần di chuyển
local function farmSwing()
	if retreating or not aimTarget or not aimTarget.Parent then
		return
	end

	local root, humanoid = getCharacterParts()

	if not root then
		return
	end

	if (aimTarget.Position - root.Position).Magnitude > attackRange then
		return
	end

	local tool = getEquippedTool(player.Character, humanoid)

	if not tool then
		statusLabel.Text = "Khong tim thay Tool!"
		releaseMouse()
		return
	end

	fireWeapon(tool, aimTarget)
end

keepUp.MouseButton1Click:Connect(function()
	setKeepDistance(keepDistance + 2)
end)

keepDown.MouseButton1Click:Connect(function()
	setKeepDistance(keepDistance - 2)
end)

task.spawn(function()
	while gui.Parent do
		local step

		if autoFarm and useWeapon then
			step = farmSwing -- vung nhanh hơn nhịp 0.4s của vòng farm
		elseif autoAttacking then
			step = attackStep
		end

		if step then
			local ok, err = pcall(step)

			if not ok then
				warn("[AutoAttack]", err)
			end
		end

		task.wait(attackCooldown)
	end
end)

-- ===== AUTO FARM =====

local farmScanRange = 5000 -- quét toàn map (bị giới hạn bởi streaming nếu game bật)
local clusterRadius = 40 -- bán kính tính "cụm"
local farmStuckTimeout = 10 -- bám 1 mục tiêu quá lâu thì bỏ qua
local farmTargetModel = nil
local farmTargetSince = 0

-- Chọn mục tiêu thuộc cụm đông nhất, có trừ điểm theo khoảng cách
local function pickCluster(root)
	local targets = collectTargets(root, farmScanRange)

	if #targets == 0 then
		return nil
	end

	local best, bestScore

	for _, candidate in ipairs(targets) do
		local count = 0

		for _, other in ipairs(targets) do
			if (other.root.Position - candidate.root.Position).Magnitude <= clusterRadius then
				count += 1
			end
		end

		local score = count * 3 - candidate.distance / 25

		if not bestScore or score > bestScore then
			best, bestScore = candidate, score
		end
	end

	return best
end

-- Hướng đứng so với mục tiêu: giữ phía hiện tại, cộng lực đẩy khỏi đám đang áp sát
local function computeAwayDirection(root, targetRoot)
	local myPos = root.Position
	local targetPos = targetRoot.Position

	-- giữ nguyên phía mình đang đứng so với mục tiêu, khỏi bay vòng vô nghĩa
	local away = (myPos - targetPos) * Vector3.new(1, 0, 1)

	away = away.Magnitude > 0.5 and away.Unit or Vector3.new(1, 0, 0)

	-- lực đẩy khỏi mọi noob đang lọt vào vùng an toàn
	local push = Vector3.zero

	for _, other in ipairs(collectTargets(root, standoffDistance * 2)) do
		local offset = (myPos - other.root.Position) * Vector3.new(1, 0, 1)
		local distance = offset.Magnitude

		if distance > 0.5 and distance < standoffDistance then
			push += offset.Unit * ((standoffDistance - distance) / standoffDistance)
		end
	end

	if push.Magnitude > 0 then
		local blended = away + push * 1.5

		if blended.Magnitude > 0.1 then
			away = blended.Unit
		end
	end

	return away
end

local function farmStep()
	if retreating then
		return -- đang treo trên trời chờ hồi máu, không kéo về mục tiêu
	end

	local root, humanoid = getCharacterParts()

	if not root then
		farmDestination = nil
		return
	end

	if not flying then
		startFly()
	end

	-- mục tiêu hiện tại còn dùng được không?
	local keepTarget = false

	if farmTargetModel and farmTargetModel.Parent and not blacklist[farmTargetModel] then
		local targetHumanoid = farmTargetModel:FindFirstChildOfClass("Humanoid")
		keepTarget = targetHumanoid ~= nil and targetHumanoid.Health > 0
	end

	-- bám mãi không hạ được (bất tử / không tới nơi) thì bỏ, cấm 20s
	if keepTarget and os.clock() - farmTargetSince > farmStuckTimeout then
		blacklist[farmTargetModel] = os.clock() + 20
		keepTarget = false
	end

	if not keepTarget then
		local pick = pickCluster(root)

		if not pick then
			farmTargetModel, farmDestination, farmAwayDir, aimTarget = nil, nil, nil, nil
			statusLabel.Text = "Farm: khong thay muc tieu"
			return
		end

		farmTargetModel = pick.model
		farmTargetSince = os.clock()
	end

	local targetRoot = farmTargetModel:FindFirstChild("HumanoidRootPart") or farmTargetModel.PrimaryPart

	if not targetRoot then
		farmTargetModel = nil
		return
	end

	farmAwayDir = computeAwayDirection(root, targetRoot)
	farmDestination = targetRoot.Position + farmAwayDir * standoffDistance + Vector3.new(0, standoffHeight, 0)
	aimTarget = targetRoot

	local distance = (targetRoot.Position - root.Position).Magnitude

	if useWeapon then
		statusLabel.Text = string.format("Farm: %s (%.0f) [%s]", farmTargetModel.Name, distance, fireMethod)
	else
		statusLabel.Text = string.format("Farm: %s (%.0f)", farmTargetModel.Name, distance)
	end

	if useWeapon then
		-- damage đi qua vũ khí của game -> server ghi nhận kill -> có coin
		local tool = getEquippedTool(player.Character, humanoid)

		if not tool then
			statusLabel.Text = "Khong tim thay Tool!"
		elseif distance <= attackRange then
			fireWeapon(tool, targetRoot)
		end
	else
		-- đủ gần để có network ownership thì kill mới ăn, không cần áp sát
		killNoobs()

		if distance <= attackRange then
			local tool = getEquippedTool(player.Character, humanoid)

			if tool then
				tool:Activate()
			end
		end
	end
end

local function setAutoFarm(state)
	autoFarm = state

	if autoFarm then
		farmToggle.Text = "FARM: ON"
		farmToggle.BackgroundColor3 = Color3.fromRGB(45, 170, 75)
	else
		farmToggle.Text = "FARM: OFF"
		farmToggle.BackgroundColor3 = Color3.fromRGB(170, 45, 45)
		releaseMouse()
		farmTargetModel = nil
		farmDestination = nil
		farmAwayDir = nil
		aimTarget = nil
		statusLabel.Text = "Target: none"
	end
end

farmToggle.MouseButton1Click:Connect(function()
	setAutoFarm(not autoFarm)
end)

task.spawn(function()
	while gui.Parent do
		if autoFarm then
			local ok, err = pcall(farmStep)

			if not ok then
				warn("[AutoFarm]", err)
			end
		end

		task.wait(0.4)
	end
end)

-- ===== VÒNG LẶP THEO DÕI MÁU =====

local function retreatStep()
	local root, humanoid = getCharacterParts()

	if not root then
		retreating = false
		retreatAnchor = nil
		return
	end

	if humanoid.MaxHealth <= 0 then
		return
	end

	local ratio = humanoid.Health / humanoid.MaxHealth * 100

	if not retreating then
		if ratio <= retreatPercent then
			retreating = true
			retreatAnchor = root.Position + Vector3.new(0, retreatAltitude, 0)
			aimTarget = nil
			releaseMouse()

			if not flying then
				startFly()
			end
		end

		return
	end

	-- hồi đủ thì quay lại làm việc
	local resumeAt = math.min(95, retreatPercent + 40)

	statusLabel.Text = string.format("Hoi mau: %.0f%% / %d%%", ratio, resumeAt)

	if ratio >= resumeAt then
		retreating = false
		retreatAnchor = nil
	end
end

task.spawn(function()
	while gui.Parent do
		local ok, err = pcall(retreatStep)

		if not ok then
			warn("[Retreat]", err)
		end

		task.wait(0.25)
	end
end)

-- ===== FULLBRIGHT =====

local fullbright = false
local lightingBackup = nil
local atmosphereBackup = {}

local function applyFullbright()
	Lighting.Ambient = Color3.fromRGB(178, 178, 178)
	Lighting.OutdoorAmbient = Color3.fromRGB(178, 178, 178)
	Lighting.Brightness = 2
	Lighting.ClockTime = 14
	Lighting.FogEnd = 1e6
	Lighting.FogStart = 1e6
	Lighting.GlobalShadows = false
	Lighting.ExposureCompensation = 0

	for _, object in ipairs(Lighting:GetChildren()) do
		if object:IsA("Atmosphere") then
			if not atmosphereBackup[object] then
				atmosphereBackup[object] = {
					Density = object.Density,
					Haze = object.Haze,
				}
			end

			object.Density = 0
			object.Haze = 0
		end
	end
end

local function setFullbright(state)
	fullbright = state

	if fullbright then
		-- lưu lại thiết lập gốc đúng 1 lần để còn trả về
		lightingBackup = lightingBackup or {
			Ambient = Lighting.Ambient,
			OutdoorAmbient = Lighting.OutdoorAmbient,
			Brightness = Lighting.Brightness,
			ClockTime = Lighting.ClockTime,
			FogEnd = Lighting.FogEnd,
			FogStart = Lighting.FogStart,
			GlobalShadows = Lighting.GlobalShadows,
			ExposureCompensation = Lighting.ExposureCompensation,
		}

		applyFullbright()

		brightToggle.Text = "LIGHT: ON"
		brightToggle.BackgroundColor3 = Color3.fromRGB(45, 170, 75)
	else
		if lightingBackup then
			for property, value in pairs(lightingBackup) do
				Lighting[property] = value
			end
		end

		for object, values in pairs(atmosphereBackup) do
			if object.Parent then
				object.Density = values.Density
				object.Haze = values.Haze
			end
		end

		atmosphereBackup = {}

		brightToggle.Text = "LIGHT: OFF"
		brightToggle.BackgroundColor3 = Color3.fromRGB(170, 45, 45)
	end
end

brightToggle.MouseButton1Click:Connect(function()
	setFullbright(not fullbright)
end)

-- game tự chỉnh lại lighting (ngày/đêm, hiệu ứng) thì ép sáng lại
task.spawn(function()
	while gui.Parent do
		if fullbright then
			pcall(applyFullbright)
		end

		task.wait(1)
	end
end)

UserInputService.InputBegan:Connect(function(input, processed)
	if processed or UserInputService:GetFocusedTextBox() then
		return
	end

	if input.KeyCode == Enum.KeyCode.F then
		toggleFly()
	elseif input.KeyCode == Enum.KeyCode.G then
		setAutoAttack(not autoAttacking)
	elseif input.KeyCode == Enum.KeyCode.H then
		setAutoFarm(not autoFarm)
	elseif input.KeyCode == Enum.KeyCode.J then
		setFullbright(not fullbright)
	elseif input.KeyCode == Enum.KeyCode.K then
		setUseWeapon(not useWeapon)
	elseif input.KeyCode == Enum.KeyCode.L then
		-- liệt kê remote để biết game gửi damage qua đâu (xem ở console F9)
		print("===== REMOTE =====")

		for _, object in ipairs(game:GetService("ReplicatedStorage"):GetDescendants()) do
			if object:IsA("RemoteEvent") or object:IsA("RemoteFunction") then
				print(object.ClassName, object:GetFullName())
			end
		end

		local character = player.Character
		local tool = character and character:FindFirstChildOfClass("Tool")

		if tool then
			print("--- trong tool", tool.Name, "---")

			for _, object in ipairs(tool:GetDescendants()) do
				print(object.ClassName, object.Name)
			end
		end

		print("==================")
	end
end)

player.CharacterAdded:Connect(function()
	aimTarget = nil
	farmDestination = nil
	farmAwayDir = nil
	farmTargetModel = nil
	retreating = false
	retreatAnchor = nil
	releaseMouse()

	if flying then
		stopFly()
	end

	-- hồi sinh xong thì farm tự bay lại
	if autoFarm then
		task.delay(1.5, function()
			if autoFarm and not flying then
				startFly()
			end
		end)
	end
end)

gui.Destroying:Connect(function()
	stopFly()
	releaseMouse()

	-- trả lighting về gốc, khỏi để map sáng trắng sau khi tắt script
	if fullbright then
		setFullbright(false)
	end
end)
